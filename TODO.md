# TODO: Fix CSS Compilation Error

- [x] Rename `resources/css/app.css` to `resources/sass/app.scss` and update its content with SCSS imports
- [x] Modify `webpack.mix.js` to use `mix.sass` instead of `mix.postCss`
- [ ] Run `npm run dev` to recompile assets
- [ ] Report the output and confirm `public/css/app.css` contains Bootstrap code
